{
  "name": "mlops-devpod",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
 
  "runArgs": [
    "--gpus",
    "all",
    "--network=host"
  ],
  
  "remoteUser": "vscode",
	"postCreateCommand": "chmod +x scripts/setup scripts/install_torch.sh && scripts/setup && scripts/install_torch.sh"
}
